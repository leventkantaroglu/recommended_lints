This packages contains recommended lint rules for scalable Flutter projects.

## Usage

Just add this package to dev_dependencies and start Dart Analyzer.

```
dev_dependencies:
  recommended_lints: ^1.0.0
```
